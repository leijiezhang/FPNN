model: 'fpn_mlp' #

device: 'cuda:0' # 'cpu' 'cuda:0'
n_batch: 3000
n_epoch: 500
n_kfolds: 5
noise_level: 0.5

n_rules: 15
lr: 0.001

dataset_folder: 'mfccs' # each kind of dataset has a same prefix name, dataset is the prefix
dataset_list:
  ['MFCCs']

#logger
log_to_file: 'true' # 'false' 'true'